<Application x:Class="ExtSync.Agent.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Design tokens shared with the web (§39): the site's dark navy theme.
                 surface-2 #0B1220 = page bg, surface #111A2E = cards, line #27334A. -->
            <SolidColorBrush x:Key="Brand" Color="#2563EB" />
            <SolidColorBrush x:Key="BrandSky" Color="#2B7DE9" />
            <SolidColorBrush x:Key="BrandTeal" Color="#0FB5BA" />
            <SolidColorBrush x:Key="Surface" Color="#111A2E" />
            <SolidColorBrush x:Key="SurfaceMuted" Color="#0B1220" />
            <SolidColorBrush x:Key="SurfaceInput" Color="#0B1220" />
            <SolidColorBrush x:Key="TextPrimary" Color="#EDF0F5" />
            <SolidColorBrush x:Key="TextMuted" Color="#94A3B8" />
            <SolidColorBrush x:Key="Success" Color="#22C55E" />
            <SolidColorBrush x:Key="Warning" Color="#F59E0B" />
            <SolidColorBrush x:Key="Danger" Color="#EF4444" />
            <SolidColorBrush x:Key="Border" Color="#27334A" />

            <!-- The site's brand gradient (blue → sky → teal). -->
            <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2563EB" Offset="0" />
                <GradientStop Color="#2B7DE9" Offset="0.55" />
                <GradientStop Color="#0FB5BA" Offset="1" />
            </LinearGradientBrush>

            <Style TargetType="Button" x:Key="PrimaryButton">
                <Setter Property="Background" Value="{StaticResource BrandGradient}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Padding" Value="14,8" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bg" Property="Opacity" Value="0.88" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bg" Property="Opacity" Value="0.45" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Quiet bordered button for secondary actions on dark cards. -->
            <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Background" Value="#1B2A47" />
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            </Style>

            <Style TargetType="Button" x:Key="DangerButton" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Background" Value="{StaticResource Danger}" />
            </Style>

            <Style TargetType="Button" x:Key="WarningButton" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Background" Value="{StaticResource Warning}" />
            </Style>

            <Style TargetType="Button" x:Key="SuccessButton" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Background" Value="{StaticResource Success}" />
            </Style>

            <!-- Dark-friendly defaults for plain controls used in dialogs. -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource SurfaceInput}" />
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                <Setter Property="BorderBrush" Value="{StaticResource Border}" />
                <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}" />
                <Setter Property="SelectionBrush" Value="{StaticResource Brand}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
